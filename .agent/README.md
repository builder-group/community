# Agent Configuration

This directory contains shared agent support files for this repository.

## Layout

- Root `AGENTS.md`: the shared top-level agent guide
- `commands/`: shared command prompts
- `rules/`: shared rules in normal Markdown
