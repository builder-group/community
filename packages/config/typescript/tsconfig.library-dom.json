{
	"$schema": "https://json.schemastore.org/tsconfig",
	"display": "Typescript configuration for TypeScript libraries that rely on DOM globals",
	"extends": "./tsconfig.library.json",
	"compilerOptions": {
		// Language and Environment
		"lib": ["esnext", "dom"]
	}
}
