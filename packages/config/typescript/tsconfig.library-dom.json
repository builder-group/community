{
	"$schema": "https://json.schemastore.org/tsconfig",
	"display": "TypeScript configuration for libraries that rely on DOM globals",
	"extends": "./tsconfig.library.json",
	"compilerOptions": {
		// Language and Environment
		"lib": ["esnext", "dom"]
	}
}
